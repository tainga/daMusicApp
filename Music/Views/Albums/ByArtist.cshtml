@model IEnumerable<Music.Models.Album>

@{
    ViewBag.Title = "Artists";
}


@{
    int counter = 0;
    foreach (var item in Model)
    {
        counter++;
    }
    if (counter == 0)
    {
        <h3>There are no albums by this artist</h3>
        <button onclick="location.href='@Url.Action("Index", "Artists")';return false;" class="btn btn-default">Back to Artists</button>
    }
    else {

<h2>Albums By Genre:  
    @foreach (var item in Model)
    {
        @Html.DisplayFor(modelItem => item.Artist.Name)
        break;
    }
</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>

    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.ActionLink(item.Title, "Details", new { id = item.AlbumID }) 
        </td>

        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GenreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GenreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GenreID })
        </td>*@
    </tr>
}

</table>
        }
    }
